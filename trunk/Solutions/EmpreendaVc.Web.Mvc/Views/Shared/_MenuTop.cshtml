@model FPN.Domain.Usuario

<ul>
    @if (Model == null)
    {
        <li> <a href="@Url.Action("Login", "Conta")">Entrar</a> |</li>
        <li> <a href="@Url.Action("Cadastro", "Conta")">Cadastrar</a> |</li>
    }
    <li> <a href="@Url.Action("Regras", "Home")">Regras</a> |</li>
    <li> <a href="@Url.Action("Contato", "Home")">Contato</a></li>
    @if (Model != null)
    {
        <li><a href="@Url.Action("Convites", "Conta")">| Convites @if (Model.Convites.Count() > 0)
                                                                { <text>(@Model.Convites.Count())</text> } | </a></li>
        <li><a href="@Url.Action("Sair", "Conta")">Sair</a></li>
    }
</ul>
